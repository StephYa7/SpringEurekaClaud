server:
  port: 10002
spring:
  application:
    name: eureka_server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:10002/eureka/